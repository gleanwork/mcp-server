name: Docker

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    name: Build Docker image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t glean-mcp-server:test .

      - name: Verify image
        run: docker run --rm glean-mcp-server:test node --version
